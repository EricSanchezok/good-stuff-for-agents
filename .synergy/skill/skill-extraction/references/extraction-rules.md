# Extraction Rules

Extract candidates only when there is enough evidence for a reusable skill-like unit.

Minimum candidate evidence:

- name or declared title;
- source path;
- format;
- trigger or usage context;
- procedure or domain instructions;
- raw metadata when available;
- content digest.

Do not normalize domains, quality, relations, or pack membership during extraction.
